import type { Meta, StoryObj } from '@storybook/react'
import Header from './Header'

const meta: Meta<typeof Header> = {
  title: 'Atomci/Organsim/Header',
  component: Header,
}

type Story = StoryObj<typeof Header>

export const Primary: Story = {}

export default meta
